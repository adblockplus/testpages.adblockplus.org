<!DOCTYPE html>
<html>
  {% set page_title = title + ' - ABP Test Pages' %}
  <? include head ?>
  <body>
    <? include header ?>
    <div class="site-container">

      {% if description %}
      <section class="site-panel">
        <h2>{{ title }}</h2>
        <p>{{ description }}</p>
      </section>
      {% endif %}

      {{ body|safe }}

    </div>
    <? include footer ?>
  </body>
</html>
